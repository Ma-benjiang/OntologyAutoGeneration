## Agent skills

### Issue tracker

Issues live in the GitHub repository configured as the current `origin` and are managed with the `gh` CLI/API. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five canonical triage labels defined for this repository. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository using root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
